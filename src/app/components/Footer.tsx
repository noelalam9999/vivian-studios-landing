import React, { useEffect, useRef } from "react";

type FooterProps = {};

const Footer: React.FC<FooterProps> = () => {
  return <div className="w-screen h-screen bg-black text-white">Footer</div>;
};
export default Footer;
